# =============================================================================
# .zshrc - Zsh 主配置文件 (chezmoi 模板)
#
# 模板变量:
#   .chezmoi.hostname  - 当前主机名 ({{ .chezmoi.hostname }})
#   .chezmoi.username  - 当前用户名 ({{ .chezmoi.username }})
#   .chezmoi.os        - 操作系统 ({{ .chezmoi.os }})
#   .is_server         - 是否为服务器 ({{ .is_server }})
#   .is_work           - 是否为工作电脑 ({{ .is_work }})
#
# 使用说明:
#   - 此文件是模板，会根据不同机器生成配置
#   - 修改请使用: chezmoi edit ~/.zshrc
# =============================================================================

# 加载所有配置模块
# 按数字顺序加载,确保依赖关系正确
if [[ -d "$HOME/.zsh" ]]; then
    for config_file in $HOME/.zsh/*.zsh(N); do
        source "$config_file"
    done
fi

# 本地个性化配置(可选,不提交到版本控制)
[[ -f "$HOME/.zshrc.local" ]] && source "$HOME/.zshrc.local"
