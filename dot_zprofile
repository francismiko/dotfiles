# =============================================================================
# .zprofile - Zsh 登录 Shell 配置
#
# 加载时机: 登录 shell 时加载（在 .zshrc 之前）
# 用途: 环境变量、PATH 配置等登录时需要的设置
# =============================================================================

# Homebrew 环境配置
eval "$(/opt/homebrew/bin/brew shellenv)"

# OrbStack 集成
# Docker 和容器管理工具
source ~/.orbstack/shell/init.zsh 2>/dev/null || :
