# =============================================================================
# Git 允许的签名者列表
#
# 格式: <email> <key-type> <public-key> <comment>
# 用途: 验证 Git 提交签名的合法性
# 管理: 由 Secretive 自动生成，使用 ssh-add -L 动态获取
# =============================================================================

1485059943@qq.com {{ output "sh" "-c" (printf "SSH_AUTH_SOCK=%s/Library/Containers/com.maxgoedjen.Secretive.SecretAgent/Data/socket.ssh ssh-add -L | head -n1" .chezmoi.homeDir) | trim }}
