# =============================================================================
# .zshrc - Zsh 主配置文件
# 最后修改: 2025-10-15
# 备份位置: ~/Obsidian-Vault/02_知识库/
# =============================================================================

# 加载所有配置模块
# 按数字顺序加载,确保依赖关系正确
if [[ -d "$HOME/.zsh" ]]; then
    for config_file in $HOME/.zsh/*.zsh(N); do
        source "$config_file"
    done
fi

# 本地个性化配置(可选,不提交到版本控制)
[[ -f "$HOME/.zshrc.local" ]] && source "$HOME/.zshrc.local"
