# 自定义函数

# 创建目录并进入
mkcd() {
    mkdir -p "$1" && cd "$1"
}

# ============================================================
# 以下为可选函数（根据需求取消注释）
# ============================================================

# # 快速查找文件
# ff() {
#     find . -type f -name "*$1*"
# }

# # 快速查找目录（注意：可能与 fd 工具冲突）
# fdir() {
#     find . -type d -name "*$1*"
# }

# # 快速提取压缩文件
# extract() {
#     if [ -f "$1" ]; then
#         case "$1" in
#             *.tar.bz2) tar xjf "$1" ;;
#             *.tar.gz)  tar xzf "$1" ;;
#             *.bz2)     bunzip2 "$1" ;;
#             *.gz)      gunzip "$1" ;;
#             *.tar)     tar xf "$1" ;;
#             *.zip)     unzip "$1" ;;
#             *.rar)     unrar x "$1" ;;
#             *)         echo "'$1' 无法提取" ;;
#         esac
#     else
#         echo "'$1' 不是有效文件"
#     fi
# }
