# =============================================================================
# PATH 配置
# 按优先级从高到低排序
#
# 注意: Homebrew PATH 配置在 .zprofile 中（登录 shell 时加载）
# =============================================================================

# 用户自定义二进制文件路径
if [[ -d "$HOME/bin" ]]; then
    export PATH="$HOME/bin:$PATH"
fi

if [[ -d "$HOME/.local/bin" ]]; then
    export PATH="$HOME/.local/bin:$PATH"
fi
